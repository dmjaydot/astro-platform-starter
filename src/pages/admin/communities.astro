---
// src/pages/admin/communities.astro
export const prerender = false;

import AdminLayout from "../../layouts/AdminLayout.astro";
import { supabase } from "../../lib/supabase";

// Auth from middleware
const isMasterKeySession = Astro.locals.isMasterKeySession || false;
const session = Astro.locals.session;

if (!isMasterKeySession && !session) {
  return Astro.redirect('/admin/login');
}

const { data: communities, error } = await supabase
  .from("communities")
  .select("id, name, slug, is_active, created_at")
  .order("created_at", { ascending: false });
---

<AdminLayout title="Communities">
  {error && <div class="alert error">{error.message}</div>}
  
  {communities && communities.length > 0 ? (
    <table class="admin-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Slug</th>
          <th>Status</th>
          <th>Created</th>
        </tr>
      </thead>
      <tbody>
        {communities.map(c => (
          <tr>
            <td>{c.name}</td>
            <td><code>{c.slug}</code></td>
            <td><span class={`badge ${c.is_active ? 'active' : 'disabled'}`}>{c.is_active ? 'Active' : 'Archived'}</span></td>
            <td>{new Date(c.created_at).toLocaleDateString()}</td>
          </tr>
        ))}
      </tbody>
    </table>
  ) : (
    <div class="panel">
      <div class="empty-state">
        <div class="empty-state-icon">💬</div>
        <div class="empty-state-title">No communities yet</div>
        <p>Communities will appear here once created.</p>
      </div>
    </div>
  )}
</AdminLayout>
